
import java.util.Iterator;

import javax.swing.JOptionPane;

public class EntradaComJOptionPane {
	/*
	 * public static void main(String[] args) { // TODO Auto-generated method
	 * stub
	 * 
	 * /* String s = JOptionPane.showInputDialog("Identifique-se Padawan...");
	 * System.out.println(s);
	 * 
	 * JOptionPane.showMessageDialog(null, "Seu nome é " +s); s =
	 * JOptionPane.showInputDialog("Entre com número inteiro..."); int i =
	 * Integer.parseInt(s); System.out.println(i);
	 * JOptionPane.showMessageDialog(null, "Seu número inteiro é "+i);
	 * 
	 * s = JOptionPane.showInputDialog("Entre com número real..."); double d =
	 * new Double(s); System.out.println(d); JOptionPane.showMessageDialog(null,
	 * "Seu número real é "+String.format("%.2f", d));
	 */

	/*
	 * String a = JOptionPane.showInputDialog("valor 1"); String b =
	 * JOptionPane.showInputDialog("valor 2");
	 * 
	 * float na = Float.parseFloat(a); float nb = Float.parseFloat(b);
	 * 
	 * int soma = (int)(na+nb); int sub = (int)(na-nb); int mult = (int)(na*nb);
	 * float div = na/nb;
	 * 
	 * JOptionPane.showMessageDialog(null, String.format("%.0f",
	 * na)+" + "+String.format("%.0f", nb)+" = "+ soma + "\n" +
	 * String.format("%.0f", na)+" - "+String.format("%.0f", nb)+" = "+ sub +
	 * "\n" + String.format("%.0f", na)+" x "+String.format("%.0f", nb)+" = "+
	 * mult + "\n" + String.format("%.0f", na)+" / "+String.format("%.0f",
	 * nb)+" = "+ div + "\n" );
	 */

	/*
	 * int y=0; //quando se tem previamente a qtde de execuções. for (int i = 0;
	 * i < 10; i++) { y =i*i; System.out.println("y=" + y);
	 * System.out.println();
	 * 
	 * }
	 */

	/*
	 * int y = 0; int x = 0; while (x <= 10) { y = x * x;
	 * System.out.println("y=" + y); System.out.println(); x++; }
	 */

	/*
	 * int x,y;
	 * 
	 * x = y = 0;
	 * 
	 * do{ y = x*x; System.out.println("y="+y); System.out.println(); x++;
	 * }while(x<=10);
	 */
	/*
	 * Double[] x = new Double[10];
	 * 
	 * x[0] = 34.65; x[9] = 1125.788; x[2] = x[0] + x[9];
	 * 
	 * System.out.println(x[2]);
	 */
	/*
	 * String[] bichanos =
	 * {"Garfield","Frajola","Felix","Tom","Braquinho","Oggy"};
	 * 
	 * for (int i = 0; i < bichanos.length; i++) {
	 * System.out.println(bichanos[i]); }
	 */

}
